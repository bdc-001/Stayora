# Copy to .env.local (gitignored). Must match backend PORT (default 5001).
VITE_API_BASE_URL=http://localhost:5001

# Stripe publishable key (pk_test_...) — required for Booking / Plan Trip payments.
# VITE_STRIPE_PUB_KEY=pk_test_your_publishable_key

# Production (Vercel): see .env.vercel.example — configure in the Vercel dashboard, not in git.
